{
  "_about": "Copy to qualien-mcp.config.json (or pass --config <path>). Servers here merge over the built-in defaults (playwright, filesystem); a key you redefine wins, and `enabled: false` turns a default off. Tools are namespaced <server>__<tool> to the host.",
  "servers": {
    "playwright": {
      "command": "npx",
      "args": ["-y", "@playwright/mcp@latest"],
      "enabled": true,
      "tools": {
        "_comment": "Optional curation — fewer tools = better host tool-selection.",
        "deny": []
      }
    },
    "filesystem": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-filesystem", "/absolute/path/to/allow"],
      "enabled": true
    },
    "github": {
      "_about": "Remote OAuth server. Each USER logs in with their OWN GitHub via `qualien-mcp login github` — a short code approved in the browser (device flow). No OAuth App to register and no client secret: qualien-mcp ships a public client id. Set `clientId` here only to point at your own OAuth App.",
      "enabled": false
    },
    "sequential-thinking": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-sequential-thinking"],
      "enabled": false
    },
    "memory": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-memory"],
      "enabled": false
    },
    "chrome-devtools": {
      "command": "npx",
      "args": ["-y", "chrome-devtools-mcp@latest"],
      "enabled": false
    }
  }
}
